# Demo Script

## 60-second recording

1. Open the dashboard and show the three fixture cards and generated signals.
2. Point to the `Demo feed` label and explain that the same UI can switch to TxLINE snapshots.
3. Click `Refresh feed` with empty credentials to show the stable demo fallback.
4. Explain the three outputs: market pressure, explainable confidence, and a readable fan narrative.
5. Close on the trading-agent track: the next adapter step is to enrich each fixture with TxLINE odds and score snapshots.

## Submission wording

MatchPulse Agent is a TxODDS-ready World Cup dashboard for the Trading Tools and Agents track. It converts football fixture context into explainable, confidence-scored signals and keeps the demo usable without requiring credentials. The adapter accepts the TxLINE guest JWT and API token for live fixture snapshots, while credentials remain in memory only.
